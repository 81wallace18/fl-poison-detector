# PFLlibMonza requirements
# Espelha env_cuda_latest.yaml + deps adicionais para o detector NLP
# (flcore/detector/cc.py, fl_save.py).
#
# Para GPU moderna (RTX 5060 Ti / arquitetura Blackwell sm_120), o torch precisa
# vir do indice CUDA 13.0 — yaml original pinava torch==2.0.1, que nao tem
# kernel pre-compilado para Blackwell. Use:
#
#   uv pip install --python .venv/bin/python -r requirements.txt \
#       --extra-index-url https://download.pytorch.org/whl/cu130
#
# Ou se ja tiver torch instalado e nao quiser mexer:
#   uv pip install --python .venv/bin/python -r requirements.txt
#
# === Core PyTorch ===
torch
torchvision

# === Detector NLP (flcore/detector/) ===
transformers
peft
safetensors
accelerate

# === PFLlib upstream (env_cuda_latest.yaml) ===
numpy
pandas
scikit-learn
scipy
ujson
h5py
seaborn
matplotlib
click
calmsize
memory-profiler
portalocker
cvxpy

